# Directus CMS Configuration
# Copy this file to .env and fill in your actual values

# Directus Instance URL (publicly accessible)
PUBLIC_DIRECTUS_URL=https://your-directus-instance.render.com

# Directus API Token (static token for read access)
# Generate this in Directus: Settings > Access Tokens > Create Token
DIRECTUS_TOKEN=your-static-api-token-here

# Optional: Admin token for build-time operations
# Only needed if you need write access during builds
# DIRECTUS_ADMIN_TOKEN=your-admin-token-here
